import {Fragment, ReactNode} from "react";
import Grid from "@mui/material/Grid2";

const WorkflowExtendedSettingsPanel: () => ReactNode = (): ReactNode => {

    return (
        <Fragment>
            <Grid
                container={true}
                padding={1}
                paddingTop={3}
                spacing={2}
                paddingBottom={2}
            >
            </Grid>
        </Fragment>
    );

}

export default WorkflowExtendedSettingsPanel;